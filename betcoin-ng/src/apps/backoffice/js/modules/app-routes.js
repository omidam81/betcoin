(function(define) {
    'use strict';
    define(['angular', 'units/bc-route'], function(angular) {
        var module = angular.module('app.routes', ['ng', 'bc.route']);
        return module;
    });
})(window.define);
